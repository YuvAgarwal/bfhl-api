{
  "name": "bfhl-api",
  "version": "1.0.0",
  "description": "BFHL API for VIT Assignment",
  "main": "api/bfhl.js",
  "scripts": {
    "start": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
